{
  "name": "atlas-rewards-mobile-shell",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor shell for the Atlas Rewards customer app (CP-76). Loads the live deployment; native plugins are bridged into the webview.",
  "scripts": {
    "sync": "cap sync",
    "android": "cap open android",
    "run:android": "cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/barcode-scanner": "^2.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.6.8",
    "@capacitor/preferences": "^7.0.0",
    "@capacitor/push-notifications": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "typescript": "^5.9.3"
  }
}
